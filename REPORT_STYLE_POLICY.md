---
status: active
owner: QA
updated: 2026-08-24
---

# [Standard] Report Style Policy — 내용과 스타일을 가른다

| Version | Date | Author | Changes |
| :--- | :--- | :--- | :--- |
| 1.0 | 2026-08-24 | QA Engineer | 신설 — 스타일 소유권 분리(2026-08-21 확정), 핸드오프 규약, 글리프 게이트 |

---

## 0. 규약 (2026-08-21 확정)

**모든 리포트는 디자이너 세션의 검토를 거쳐 발행한다.**
리포트를 **쓰는** 세션(QA · dev · planner)은 **내용에만 집중하고 스타일을 직접 짜지 않는다.**

> **소급 적용하지 않는다.** 이미 발행된 리포트를 되돌아가 리디자인하지 않는다.
> 규약은 **앞으로 나오는 리포트**에만 걸린다.

**Why.** 리포트마다 스타일이 제각각이면 읽는 쪽이 매번 새로 학습한다. 그리고 색 대비·색약
판정은 눈으로 못 하고 검증기를 돌려야 하며, **인쇄(PDF)는 화면과 규칙이 다르다.**
이 판단을 한 세션에 모으는 것이 목적이다. 역할 경계는
[`SESSION_ROLES.md`](./SESSION_ROLES.md) §5.

---

## 1. 무엇을 쓰고 무엇을 안 쓰나

| | 내용 세션 (QA·dev·planner) | 디자인 세션 |
| :--- | :--- | :--- |
| 문장 · 수치 · 판정 | ✅ 소유 | ❌ 안 건드림 |
| 표 · 경고 블록 · 핵심 수치 · 판정 배지 **의 구조 신호** | ✅ 마크업으로 남김 | — |
| `<style>` · 인라인 `style=` · 임의 hex | ❌ **금지** | ✅ 소유 |
| 팔레트 · 타이포 · 여백 · 페이지 분할 | ❌ | ✅ 소유 |
| 이모지 · 딩벳 글리프 | ❌ **금지** (§3) | ✅ 승인된 목록만 |

**내용 세션이 남기는 것은 「구조 신호」뿐이다.** 이게 표인지 경고인지 판정인지만 밝히고,
그게 무슨 색으로 얼마나 크게 나올지는 안 정한다.

```html
<!-- ✅ 내용 세션이 쓰는 것 — 클래스만 -->
<div class="flag"><b>NFC 오탐</b> 물리 이슈 미해결인데 정상 복구 알림 발송.</div>
<td class="res-fail">FAIL</td>

<!-- ❌ 내용 세션이 쓰면 안 되는 것 -->
<div style="background:#FEF9E7;border-left:4px solid #F1C40F;padding:10px">…</div>
<td style="color:#CD1C1C;font-weight:700">FAIL</td>
```

---

## 2. 공용 CSS 1벌

스타일의 **원본은 한 파일**이다. 이 저장소에서는
[`assets/report.css`](./assets/report.css) — A4 인쇄 전용.

- 리포트 HTML 은 `<link rel="stylesheet" href="assets/report.css">` 한 줄만 갖는다
- **리포트마다 CSS 를 복사하지 않는다.** 복사하는 순간 어느 쪽이 원본인지 알 수 없게 된다
- 팔레트를 손대면 **대비·색약 검증기를 반드시 재실행**한다
- 컴포넌트가 부족하면 인라인으로 때우지 말고 **공용 CSS 에 추가**한다

> 팀에 자체 디자인 시스템이 있으면 `assets/report.css` 를 그 자산으로 교체한다.
> 규약(내용/스타일 분리)만 그대로 쓴다.

---

## 3. 글리프 게이트 (발행 전 필수)

리포트는 **A4/PDF 로 인쇄**되므로 폰트에 없는 문자가 조용히 사라진다.
**`−`(U+2212)는 ASCII 하이픈과 육안 구분이 안 돼 사람이 검토해서는 절대 못 잡는다.**

발행 전 **두 층위를 다 돌린다** — 소스 HTML(유입 차단) + 산출 PDF(최종 확인).
파이프라인마다 죽는 글자가 다르고, self-test 없는 감사는 0건이 「깨끗함」인지 「못 읽음」인지
구분하지 못한다. 상세 절차·함정은 [`VERIFICATION_GUIDE.md`](./VERIFICATION_GUIDE.md) §4.

---

## 3-1. 각주·주석의 색과 성질 (2026-09-10)

각주의 성질을 **색으로만** 말하지 않는다 — 색은 흑백 인쇄·텍스트 추출에서 통째로 사라진다.

- **앰버(경고색)는 「조치가 필요한 것」에만.** 정보·정상은 **회색**. 「이런 게 있다」(사실)와
  「고쳐질 것」(한시적 결함)을 같은 앰버로 칠하면 둘 다 경고로 읽힌다.
- **성질을 문장 안에 넣는다** — 「(정상)」·「…사라집니다」·「…확인해야 합니다」. 색이 죽어도
  문장이 남고, **문장 안의 성질 표지가 기계가 잴 수 있는 유일한 축**이다(검사가 색은 못 본다).
- **강조는 색이 아니라 활자(볼드)로.** 붉은 활자는 안내여도 경고로 읽힌다 — 「Critical 최다:
  CDK2」의 강조는 장비명 볼드로 옮기고 색은 회색으로.
- **색 대비는 활자와 면을 따로 잰다.** 같은 색이라도 글자 획(활자)은 가늘어 배경과 섞이고
  면(막대·배지)은 색 그대로 나온다 — 흑백에서 판정이 반대로 갈린다. 판정엔 **그 색이 실제로
  쓰이는 층**의 값을 쓰고, 한 요소가 둘 다면(틴트 배지 위 글자) 둘 다 통과해야 한다.
- **색으로 나눌 수 있는 상태 수에는 상한이 있다 — 취향이 아니라 수다.** 대비비는 곱으로
  이어지고(검정↔흰 21:1이 천장) 마크는 배경 위에 있어 흰 배경이면 가장 밝은 마크도 배경
  대비 3:1이 필요하다 — 남는 구간이 7배뿐이라 **흰 배경 위 마크 하나로 3:1을 지키며 나눌
  수 있는 상태는 최대 둘.** 넘으면 「팔레트를 다시 고르면 된다」가 답이 아니라 **명도 아닌 축**
  (모양 `●`↔`○`·채움/윤곽·위치)을 하나 더 연다. **흑백 판정은 601(0.299/0.587/0.114)이
  아니라 상대휘도(WCAG)로** — 601은 초록 가중이 낮아 초록을 실제보다 어둡게 보여 분리를
  과대평가한다(초록 잉크가 601로 14 떨어져 보여도 실제 1.02:1, 같은 회색). 구체 공식·측정
  스크립트 정본은 디자인 세션 KB.
- **「색이 안 갈린다」는 두 급으로 가른다 — 소실과 마찰.** 색이 **유일한 채널**이면 흑백에서
  정보가 사라진다(소실). 옆에 제목 활자·축 라벨·상태 글자가 **중복 부호**로 있으면 읽기가
  더뎌질 뿐 정보는 남는다(마찰). 둘을 같은 급으로 올리면 **진짜 소실이 마찰 더미에 묻힌다** —
  우선순위는 소실 먼저.

## 4. 핸드오프 규약

### 4.1. 내용 세션 → 디자인 세션 (5종 세트)

넘길 때 아래 다섯을 **반드시 같이** 준다. 하나라도 빠지면 디자인이 추측으로 채운다.

| # | 항목 | 예 |
| :--- | :--- | :--- |
| 1 | **파일 경로** | `Report/10.6th_Hotfix_QA_Report_2026-08-20.html` |
| 2 | **문서 성격** | 판정형 / 논증형 / 집계형 |
| 3 | **독자** | 개발팀 리드 + 고객 담당 |
| 4 | **주인공 정보 한 줄** | "판정이 PASS 라는 것" · "실패율이 3%p 내려갔다는 것" |
| 5 | **페이지 제약** | A4 5쪽 이내 |

**문서 성격이 컴포넌트를 고른다** — 판정형은 판정 배지·검증 표, 논증형은 근거 블록·인용,
집계형은 KPI 타일·차트. **수치가 논지면 KPI 타일·바로 승격**한다.

### 4.2. 디자인 세션이 지키는 것

- **원본을 덮지 않는다.** `_redesign` 접미사로 별도 생성
- **내용·수치를 고치지 않는다.** 이상하면 쓴 세션으로 되돌린다
- 넘기기 전 글리프 감사 self-test 통과(§3)
- 렌더 후 **페이지 수 비교 + 스크린샷 육안 확인**

### 4.3. 수치를 넘길 때

**「잰 것(자동 집계)」인지 「본 것(자기 보고)」인지 라벨을 붙인다.**
한 번은 넘긴 자료 5개 중 **틀린 2개가 전부 자기 보고**였다.

---

## 5. 적용 범위 경계 (중요)

이 규약은 **HTML/CSS 로 만드는 인쇄 산출물**에 건다. 아래는 **대상이 아니다.**

| 대상 아님 | 이유 |
| :--- | :--- |
| **자체 렌더 파이프라인을 가진 제품 PDF** (PDF 라이브러리 + 차트 라이브러리 직접 생성) | CSS 를 안 쓴다. 자체 설계 명세가 기획·QA 와 함께 관리된다 |
| **저장소 내부 마크다운** (진행 기록·명세) | 에디터로 읽는 버전관리 문서다 |
| **브라우저 전용 화면** (블로그·웹 대시보드) | 인쇄 제약이 없고 폰트 폴백이 넓다 |

경계를 안 그으면 **CSS 를 안 쓰는 산출물에 CSS 규약을 강요**하게 된다.

---

## 6. 기존 문서와의 관계

- [`HOTFIX_REPORT_TEMPLATE.md`](./HOTFIX_REPORT_TEMPLATE.md) — 이 규약을 따르는 템플릿.
  §3 HTML 은 **클래스만** 쓰고 스타일은 `assets/report.css` 에 있다.
- [`QA_REPORT.md`](./QA_REPORT.md) §11 — PDF 산출 룰. 스타일 원본 지정은 이 문서로 넘겼다.
- [`SESSION_ROLES.md`](./SESSION_ROLES.md) §5 — 디자인 세션의 역할·경계.
