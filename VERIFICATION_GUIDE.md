---
status: active
owner: QA
updated: 2026-08-24
---

# [Standard] Verification Guide — 검사가 실제로 무언가를 보고 있는가

| Version | Date | Author | Changes |
| :--- | :--- | :--- | :--- |
| 1.0 | 2026-08-24 | QA Engineer | 신설 — §1 눈 없는 단언 · §2 원본을 돌린다 · §3 무변경은 해시로 · §4 최종 매체 감사 |

---

## 0. 이 문서가 푸는 문제

TC 를 잘 쓰는 것과 **그 TC 가 실제로 무언가를 보고 있는 것**은 다른 문제다.
초록이 「통과했다」인지 「아무것도 안 봤다」인지는 **결과가 같아서 구분되지 않는다.**

`TC_DESIGN_GUIDE.md` 가 *무엇을 검증할까*를 다룬다면, 이 문서는 *그 검증을 믿을 수 있나*를
다룬다. 유닛 테스트 · E2E · 자동화 감사 · 남의 결과 재확인 전부에 적용된다.

---

## 1. 검사는 실패할 수 있어야 한다

**규칙: 새로 쓴 검사는 반드시 한 번 일부러 실패시켜 본다.**
실패하지 않으면 그 검사는 아무것도 보고 있지 않다.

한 번은 E2E 에서 이 절차 하나로 **8건**이 걸렸다. 안 했으면 전부 초록으로 남았다.
결정적 증거는 같은 화면 같은 시점에 `toHaveCount(0)` 과 `toBeVisible()` 이 **둘 다 통과**한
것이었다 — 서로 모순인 두 단언이 함께 참일 수는 없다.

### 1.1. 눈 없는 단언 — 7가지 전형

| # | 유형 | 실제 사례 |
| :--- | :--- | :--- |
| 1 | **없는 문구의 부재를 단언** | `/master data/i` 를 찾는데 실제 제목은 `Admin` |
| 2 | **일시적인 것을 부재로 봄** | 적어도 75ms 는 떠 있는 요소인데 폴링이 리다이렉트 뒤에 떨어짐 |
| 3 | **존재하지 않는 요소를 셈** | `table tbody tr` 인데 그 화면에 `<table>` 이 없음 |
| 4 | **집계로 확인 — 집계가 구분을 지움** | 아래 §1.2 |
| 5 | **재는 대상에 도달하지 못함** | 아래 §1.3 |
| 6 | **0 이 「없음」인지 「못 봄」인지 구분 안 됨** | 아래 §1.4 |
| 7 | **「같은 것의 다른 표현」 중 한 형태만 봄** | 아래 §1.5 |

### 1.2. 유형 4 — 집계가 구분을 지운다

하루에 네 번 난 적이 있다. **전부 값은 맞았다. 재는 방법이 그 값을 확인할 수 없었을 뿐이다.**

- `sum(x or 0) == 0` 이 **NULL 과 0 을 구분 못 했다** → "전부 NULL" 이라는 오보를 며칠 믿었다
- "9대 평균 == KPI" 가 맞았는데, **구성이 다른 두 장비 집합의 대수가 우연히 둘 다 9**였다
- "괄호합 == 총합" 이 맞았는데, **아무것도 안 잘렸기 때문**이었다
- "기종별 합 == KPI" 가 맞았는데, **혼합 그룹이 0개인 그 데이터에서만** 참이었다

→ **「값이 맞는다」는 확인에는 어떤 모집단·어떤 데이터에서 맞는지를 같이 적는다.**
`IS NULL` 로 직접 세는 것과 `sum(… or 0)` 을 보는 것은 **다른 질문이다.**

### 1.3. 유형 5 — 그 분기를 아예 안 지났다

앞의 넷과 결이 다르고 더 잡기 어렵다. 앞은 「재는 방법이 구분을 못 한 것」이고
이건 **「그 경로를 한 번도 안 지난 것」**이다.

이월 INSERT 분기를 검증하려고 `prefill + carry` 로 데이터를 만들었는데 **carry 가 UPDATE
분기로 갔다.** "줄 나온다, 통과"로 끝냈으면 **막으려던 함정을 정확히 비껴간 채 초록**이다.
커버리지가 초록인데 그 분기 실행 횟수가 0인 상황과 같다.

→ **검사가 그 분기를 실제로 지났는지 따로 센다.** 분기 판별값을 출력에 찍고,
분기를 강제하는 입력을 일부러 만든다(위 사례는 prefill 을 비워 INSERT 를 강제).

### 1.4. 유형 6 — 0 이 「없음」인가 「못 봄」인가

글리프 감사에서 `<style>` 제외 필터가 본문까지 삼켜 위반 **0건**이 나왔다.
그 0 은 「깨끗함」이 아니라 **「아무것도 안 읽음」**이었다.

같은 감사가 반대로도 실패했다 — CSS 주석 배너의 박스드로잉이 **610건**으로 잡혀
진짜 위험 1건을 덮었다. **과다검출과 미검출은 같은 급의 실패다.** 둘 다 결과를 볼 수 없게 만든다.

→ **입력이 실제로 읽혔는지를 따로 단언한다.** 본문 길이가 원문의 15% 미만이면
PASS 가 아니라 **BROKEN**. PDF 도 같다 — 추출 길이 0 은 「글리프 없음」이 아니라
**파서가 그 형식을 못 읽음**일 수 있다.

### 1.5. 유형 7 — 동치 표현 중 한 형태만 본다

글리프 감사가 리터럴 `−` 는 잡는데 `&#x2212;` 로 쓰면 통과했다.
**렌더 결과는 완전히 같다 — 소스 표현만 다르다.** self-test 도 리터럴만 심어서
그 경로를 한 번도 안 지났다(유형 5 와 겹친다).

→ 검사가 보는 게 **표현**인지 **결과**인지 먼저 정한다. 표현을 본다면 **동치 표현을 전부
열거**해 각각 심어서 잡히는지 본다. E2E 로 옮기면: 같은 화면을 텍스트·role·label·test-id 로도
가리킬 수 있다 — 한 경로만 막고 "막았다"고 하면 나머지가 열려 있다.

덤: 재현 케이스를 짧게 만들었더니 **sanity 가드가 먼저 걸려 검출 자체를 못 봤다.**
**가드가 검증을 가리는 형태**도 있다 — 최소 재현이 가드 임계 아래로 내려가지 않게 만든다.

### 1.6. 빨강도 초록과 같은 무게로 의심한다

사본 렌더 하네스가 실제 라우트의 주입 한 단계를 빠뜨려 PDF 2개 페이지를 DIFF 로 만들었다.
**코드는 멀쩡했고 재는 쪽이 틀렸다.** 그대로 보고했으면 없는 회귀를 쫓느라 배포가 멈춘다.

→ **재현용 하네스는 실제 경로가 하는 일을 전부 흉내 내야 증거가 된다.**

### 1.7. 가드는 양쪽을 다 확인한다

「죽어야 할 것이 죽는다」만 보면 절반이다. **「살아야 할 것이 산다」**를 안 보면 정상 입력이
죽고, 그 다음에 사람이 하는 일은 **임계를 낮추는 것**이라 가드가 원래 막으려던 구간을
스스로 통과시킨다.

→ 하드 실패는 **틀릴 수 없는 조건**에만 건다. 임계값은 데이터가 바뀌면 같이 틀리지만
"본문 태그를 못 찾았다"는 안 틀린다. 비율 같은 값은 **경고로만** 낸다.

### 1.8. 체크리스트

- [ ] **단언을 쓰기 전에 「이 검사가 빨강이 되는 입력이 무엇인가」에 답한다.** 답이 없으면
      검사가 아니라 **통과를 보장하는 문장**이다
- [ ] 빨강을 만들 수단이 없으면 **검사를 붙이지 않는다.** 커버리지만 오른 것처럼 보여 더 나쁘다
- [ ] 부재만 단언하지 않는다. 긍정·부재를 짝짓는다 — 「A 가 보이고 B 가 없다」
- [ ] 일시적인 상태는 보는 게 아니라 **기록**한다(이벤트 훅·프레임별 표본)
- [ ] 반전시켜서 통과하면 "제품이 그렇구나"보다 **"내 단언에 눈이 없구나"**를 먼저 의심한다
- [ ] 검사가 그 분기를 지났는지 **따로 센다**
- [ ] 입력이 읽혔는지를 **따로 단언한다**

### 1.9. 세 얼굴 (외우는 형태)

전부 「초록인데 아무것도 안 본다」다.

1. **검사가 빨강을 낼 수 있나** — 일부러 실패시켜 본다
2. **검사가 진짜 대상을 보고 있나** — **최종 매체**에서 본다(화면이 아니라 배포되는 PDF)
3. **0 이 「없음」인가 「못 봄」인가** — 입력이 읽혔는지를 따로 센다

> 같은 함정이 **진단·계측 코드**에서도 난다. 거기는 초록/빨강이 없어 아무도 안 알려준다.
> 로그 문자열 자체가 죽는 경우도 있다 — cp949 콘솔에서 `—`(em dash)가 인코딩 예외를 내
> **하필 그 경고가 필요한 순간에 메시지가 안 남았다.** 로그는 ASCII 안전하게 쓴다(한글은 OK).

---

## 2. 검증은 원본을 돌린다 — 재구현 금지

남의 코드·명세·보고를 검증할 때 **재구현하지 않는다.** 배포된 파일에서 함수 본문·표현식을
**그대로 떼어내** 실제 설정 모듈과 함께 실행한다.

**Why.** 재구현하면 「저쪽이 어떻게 동작하나」가 아니라 **「내가 어떻게 읽었나」**를 검사하게
된다. **상대의 오독과 내 오독이 같으면 둘 다 통과한다.**

한 번은 이 방식이 기획 명세의 결함 **셋**을 잡았다. 전부 「읽으면 맞아 보이는」 조각이었다.

- 인용된 `have = all(… for i in items for c in cols)` 를 돌려 보니 `all()` 이 **빈 제너레이터에
  True** 라, 식을 줄이는 과정에서 `if items else False` 가 조용히 뒤집혔다
- 같은 식이 NULL 이 섞인 행 하나에 **출력 줄을 통째로 없앴다** — 배포 순서 함정
- "N행 전부 NULL" 이라는 보고가 실은 `sum(… or 0) == 0` 이었다. **NULL 과 0 을 못 가른다**

### 2.1. 절차

1. **함수 통째 import 가 막히면**(의존 패키지 미설치 등) **소스에서 그 블록만 잘라 실행한다.**
   설정 상수는 **진짜 모듈을 물린다** — 상수까지 손으로 옮기면 재구현으로 돌아간다.
2. **작업 트리가 dirty 면 `git show <배포ref>:<파일>` 사본으로만 판정한다.**
   작업 트리 파일을 쓰면 **배포 안 된 코드를 검증한 게 된다.**
3. **쓰기가 필요한 검증은 격리 리그로.** 빈 DB 를 새로 만들고 **실제 저장 모듈을 import 해
   진짜 함수를 호출**한다. **운영 DB 에는 만들지 않는다** — 그 데이터가 실제 회의 자료다.
4. **리그에 「그 경로를 지났는가」 계측을 박는다**(§1.3).
5. **다시 쓸 리그는 세션 스크래치패드에 두지 않는다.** 세션과 함께 사라진다.

### 2.2. 「로그가 안 찍힌다」의 원인은 셋

① 콘솔 인코딩(cp949) · ② 실제 미출력 · ③ **재는 쪽의 버퍼링**
(stderr 는 무버퍼, 파이프의 stdout 은 블록버퍼 → `2>&1 | tail` 이 로그를 앞으로 밀어 자른다).

→ **먼저 무버퍼 실행 + stderr 분리로 셋을 갈라라.** 원인을 안 가르고 "로그가 없다"를
결론으로 쓰면 없는 문제를 쫓는다.

### 2.3. 값이 맞는 것과 구조가 보장하는 것을 갈라 쓴다

**「값도 맞다 — 다만 우연히 맞는 것」은 통과가 아니다.**
여집합 정의처럼 **구조로 보장**되면 그렇다고 명시한다. 데이터 의존이면 그것도 명시한다.
안 갈라 놓으면 다음 사람이 그 등식을 불변식으로 믿는다.

---

## 3. 무변경 리팩터는 해시로 증명한다

출력이 안 바뀌어야 하는 변경(리팩터·중복 제거)은 **산출물을 바이트로 비교해서 증명한다.**
PDF 는 페이지별 PNG 해시, HTML 은 바이트 수·해시, 차트는 PNG 해시.

**Why.** 차트·레이아웃은 1픽셀씩 밀려도 눈으로는 같아 보인다. **「같아 보인다」는 관측이지
증명이 아니고**, 밀린 걸 나중에 발견하면 그 사이 커밋 **전부가 용의자**가 된다.

**How to apply**
- 변경 **전** 라이브 산출물을 **먼저 받아 둔다.** 나중에 뜨면 비교 대상이 없다
- 시각이 박히는 산출물(`Generated ...`)은 텍스트 추출 diff 로 **「그 한 줄만 다르다」**를 같이 보인다
- **레이아웃이 움직이는 작업은 리팩터가 아니다.** 같은 커밋에 섞지 않는다 —
  섞는 순간 방금 세운 "안 바뀌었다"를 스스로 깬다
- **기준선 이름이 비슷하면 갈라 적는다.** 「데이터 변형 감지용 지문」과 「표시 회귀용 기준선」은
  별개다. 표시 코드를 고칠 때마다 데이터 지문을 재산출하면 **지문이 지문이 아니게 된다** —
  진짜 데이터 변형도 "또 표시 바뀌었나 보다"로 넘어간다

> **표시 결함은 원인이 규칙 중복일 때가 있다.** "`…` 가 안 붙는다"의 실체가 "자르고 `…` 붙이는
> 규칙이 세 벌이라 한 벌이 빠졌다"였다. 표시만 고치면 다음에 또 한쪽만 고쳐진다 —
> **고칠 층위를 한 칸 내려서 볼 것.**

---

## 4. 최종 매체에서 감사한다 — 글리프·렌더

§1.9 의 두 번째 얼굴을 산출물에 적용한 것.

**판정 층위가 갈린다.** 화면 스크린샷에서 멀쩡히 보인 문자가 **PDF 텍스트에는 없었다.**
**최종 매체 실측이 우선**이다 — 화면에서 보이는 건 증거가 아니다.

### 4.1. 파이프라인마다 결과가 다르다 — 짐작으로 고를 수 없다

같은 문자가 한 파이프라인에선 살고 다른 쪽에선 죽는다. **한쪽 표를 다른 쪽에 쓰면 안 된다.**
자체 폰트를 임베드하는 PDF 라이브러리와 시스템 폰트로 폴백하는 브라우저 인쇄는 커버리지가 다르다.

**새 매체·새 기호를 도입하면 프로브를 새로 돌린다.**
프로브 = 후보 글자를 전부 담은 HTML → 최종 매체로 렌더 → **추출해서 살아남은 것을 센다.**

> 실측 예 (브라우저 headless 인쇄 파이프라인): `−`(U+2212) · `⚡`(U+26A1) · `✓`(U+2713) 는
> **PDF 에서 사라졌고**, `→ ÷ ↳ ※ ① ② ⟳ ✗ — ↑ ↔` 는 정상이었다.
> `✗`(U+2717)는 살고 `✓`(U+2713)는 죽는다. 이 표는 **그 파이프라인에서만** 유효하다.

### 4.2. `−`(U+2212)는 위험 등급이 다르다

`⚡`·`⟳` 는 눈에 띄어서 사람이 보면 안다. **`−` 는 ASCII 하이픈과 시각적으로 구분이 안 된다.**
**눈으로 검토해서는 절대 못 잡고 자동 글리프 검사로만 걸린다.** 실제로 회귀가 났다.
→ "발송 전 육안 검수 1회" 로는 이 결함을 못 잡는다는 뜻이다.

### 4.3. 감사 도구는 두 층위, 둘 다 돌린다

| 층위 | 보는 것 | 목적 |
| :--- | :--- | :--- |
| **소스 HTML** | 리터럴 + **동치 엔티티**(`&#x2212;` `&minus;`) | 유입 차단 |
| **산출 PDF** | 실제 추출된 문자 | 최종 확인 |

- **`<style>` 블록은 제외한다.** CSS 주석 배너의 박스드로잉이 610건을 뱉어 진짜 위험 1건을 덮는다.
  단, **제외 필터가 본문까지 삼키지 않았는지 §1.4 대로 따로 단언**한다.
- **self-test 를 붙인다** — 금지 글자를 일부러 심어 검사가 잡는지 먼저 증명하고, 못 잡으면 죽는다.
  **동치 표현마다 심는다**(§1.5).
- **음성 대조용 프로브 파일을 하나 둔다.** 죽는 글자를 일부러 담고 있어 **FAIL 이 정상**인 파일.
  이게 PASS 로 바뀌면 검사가 눈을 잃은 것이다.

### 4.4. 렌더 스크립트 함정

인쇄 스크립트에 **파일명 인자를 반드시 준다.** 인자 없이 돌려 **폴더 전체를 재렌더**해
다른 사람의 산출물까지 덮어쓴 사고가 있다.

**적용 범위.** tofu 위험은 **A4/PDF 로 인쇄되는 산출물에만** 있다.
브라우저 렌더(블로그 등)와 저장소 내부 마크다운은 대상이 아니다.

---

## 5. 남의 결과를 받았을 때

peer 세션·동료·자동화가 준 결론은 **입력이지 사실이 아니다.**

| 받은 형태 | 확인할 것 |
| :--- | :--- |
| "N행 전부 NULL" | 어떻게 셌나 — 집계인가 `IS NULL` 인가(§1.2) |
| "회귀 났다" | 재현 하네스가 실제 경로를 전부 흉내 냈나(§1.6) |
| "검증 돌렸고 통과" | **그 분기를 지났나**(§1.3) |
| "값이 일치한다" | 어떤 데이터에서? 구조 보장인가 우연인가(§2.3) |
| "육안 확인했다" | 최종 매체에서 봤나(§4) · **육안으로 잡히는 종류인가**(§4.2) |
| 수치 일반 | **「잰 것」인가 「본 것」인가** — 라벨 없으면 되묻는다 |

한 번은 넘어온 자료 5개 중 **틀린 2개가 전부 자기 보고(본 것)**였다.
잰 것은 틀리지 않았다. **라벨이 곧 신뢰도다.**
